#include "main.h"

/**
 *_isupper - cheks for uppercase letters
 *@c: character to be cheked
 *
 * Return: give 0 (success)
 */
int _isupper(int c)
{
	int i;

	for (i == 'A'; i <= 'Z'; i++)
	{
		if (c == i)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
