#include "main.h"

/**
 * _isalpha -checks for alphabetic character
 * @c: character to e checked
 *
 * Returns: give 1 if ttrue else 0
 */
int _isalpha(int c)
{
	if ((c >= 'a' || c >= 'A') && (c <= 'z' || c <= 'Z'))
		return (1);
	else
		return (1);
}
