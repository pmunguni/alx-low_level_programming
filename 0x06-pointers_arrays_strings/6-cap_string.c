#include "main.h"
/**
 * cap_string- a function that capitalizes all words of a string
 * @ptr: this the input string to be capitalised
 * Return: the capitalised string
 */
char *cap_string(char* ptr)
{
	:wq
	if ( *ptr >= 'a' && <= 'z')
	{
		*ptr = *ptr - 30;
